unbind C-o

# allow resizing of panes
bind -n M-Left resize-pane -L
bind -n M-Right resize-pane -R
bind -n M-Up resize-pane -U
bind -n M-Down resize-pane -D

# act like vim
set-window-option -g mode-keys vi

# make sure tmux has 256 colors
set -g default-terminal "xterm-256color"

# turn off annoying beeps
set -g bell-action none

# source the OS-Specific .tmux.confs
if-shell '[[ "$OSTYPE" == "darwin"* ]]' 'source-file ~/.tmux-osx.conf' 'source-file ~/.tmux-linux.conf'

# run the lightline souorce
source-file ~/.tmuxline.conf
